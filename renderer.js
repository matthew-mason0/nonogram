class Renderer {
    constructor(ctx, board) {
        this.ctx = ctx;
        this.board = board;
    }

    drawBoard() {
        const rows = this.board.rows;
        const ctx = this.ctx;
        for (let i = 0; i < rows; i++) {
            for (let j = 0; j < rows; j++) {
                
            }
        }
    }
}